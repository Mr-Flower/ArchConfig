#%PAM-1.0

auth       required                    pam_faillock.so      preauth

# --- INIZIO LOGICA IMPRONTA ---
# Se lo script check_lid.sh fallisce (PC chiuso), salta la riga successiva (default=1)
auth       [success=ignore default=1]  pam_exec.so quiet /usr/local/bin/check_lid.sh
# Se il PC è aperto, prova l'impronta. Se funziona, l'autenticazione è fatta (sufficient)
auth       sufficient                  pam_fprintd.so
# --- FINE LOGICA IMPRONTA ---

-auth      [success=2 default=ignore]  pam_systemd_home.so
auth       [success=1 default=bad]     pam_unix.so          try_first_pass nullok
auth       [default=die]               pam_faillock.so      authfail
auth       optional                    pam_permit.so
auth       required                    pam_env.so
auth       required                    pam_faillock.so      authsucc

-account   [success=1 default=ignore]  pam_systemd_home.so
account    required                    pam_unix.so
account    optional                    pam_permit.so
account    required                    pam_time.so

-password  [success=1 default=ignore]  pam_systemd_home.so
password   required                    pam_unix.so          try_first_pass nullok shadow
password   optional                    pam_permit.so

-session   optional                    pam_systemd_home.so
session    required                    pam_limits.so
session    required                    pam_unix.so
session    optional                    pam_permit.so
